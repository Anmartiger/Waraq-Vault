# Extra build-time deps for freezing the backend into the Tauri sidecar.
# Not needed to run the app from source or in Docker — see requirements.txt.
pyinstaller==6.21.0
pyinstaller-hooks-contrib==2026.6
